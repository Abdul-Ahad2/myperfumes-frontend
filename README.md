### Frontend of MyPerfumes website
In order to start the frontend run these commands in the terminal:\
`npm install` to install all the packages used\
`npm run dev` to run the frontend server on localhost 
